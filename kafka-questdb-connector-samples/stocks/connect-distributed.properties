# Kafka Connect worker configuration, see https://kafka.apache.org/documentation/#connectconfigs
bootstrap.servers=kafka:9092
group.id=connect-cluster
listeners=http://:8083

# Where the worker looks for connector plugins, see Dockerfile-Connect
plugin.path=/opt/kafka/plugins

# Default converters; both connectors in this sample override these in their own config
key.converter=org.apache.kafka.connect.json.JsonConverter
value.converter=org.apache.kafka.connect.json.JsonConverter

# Internal topics; replication factor 1 because the sample runs a single broker
offset.storage.topic=connect-offsets
offset.storage.replication.factor=1
config.storage.topic=connect-configs
config.storage.replication.factor=1
status.storage.topic=connect-status
status.storage.replication.factor=1
